server:
  port: 8086
  servlet:
    session:
      tracking-modes: cookie
      timeout: 1800

spring:
  application:
    name: email
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8071